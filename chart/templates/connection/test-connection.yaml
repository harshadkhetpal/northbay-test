apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-bluegreen.fullname" . }}-test-connection"
  labels:
    helm.sh/hook: test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['nginx-service:{{ .Values.service.port }}']
  restartPolicy: Never
