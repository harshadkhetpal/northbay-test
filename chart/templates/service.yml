apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  type: {{ .Values.service.type }}
  selector:
    app: nginx
    version: {{ .Values.activeVersion }}  
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
